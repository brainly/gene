import React from 'react';
import { InvalidModule } from './InvalidModule';

export default {
  title: 'invalid-module',
};

export const DefaultView = () => <InvalidModule />;

DefaultView.story = {
  name: 'default view',
};
