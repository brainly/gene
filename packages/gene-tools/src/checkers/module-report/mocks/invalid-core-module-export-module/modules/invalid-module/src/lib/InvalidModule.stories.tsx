import React from 'react';
import { InvalidModule } from './InvalidModule';

export default {
  title: 'invalid-module',
};

export const CoreDefaultView = {
  render: () => <InvalidModule />,
  name: 'core - default view',
};
