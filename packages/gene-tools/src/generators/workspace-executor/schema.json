{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "workspace-executor",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the workspace executor.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
